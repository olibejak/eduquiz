$kind: http-request
name: Start quiz
url: "{{baseUrl}}/api/quiz/{{quizPin}}/start?hostPlaying=true"
method: POST
headers:
  Quiz-Token: "{{quizToken}}"
queryParams:
  hostPlaying: "true"
scripts:
  - type: afterResponse
    code: |-
      pm.test("Status is 200 OK", function () {
          pm.response.to.have.status(200);
      });
    language: text/javascript
order: 4471932855774118
