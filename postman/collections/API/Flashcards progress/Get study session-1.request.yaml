$kind: http-request
name: Get study session
url: "{{baseUrl}}/api/flashcards/{{deckId}}/session?size=20"
method: GET
queryParams:
  size: "20"
scripts:
  - type: afterResponse
    code: |-
      pm.test("Status is 200 OK", function () {
          pm.response.to.have.status(200);
      });
    language: text/javascript
order: 758443643327689
