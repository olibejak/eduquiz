$kind: http-request
name: Revert update user
url: "{{baseUrl}}/api/users/me"
method: PATCH
body:
  type: json
  content: |-
    {
      "username": "{{username}}",
      "email": "{{email}}"
    }
scripts:
  - type: afterResponse
    code: |-
      pm.test("Status is 200 OK", function () {
          pm.response.to.have.status(200);
      });
    language: text/javascript
order: 3074457359049081
