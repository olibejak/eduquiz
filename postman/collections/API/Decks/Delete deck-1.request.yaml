$kind: http-request
name: Delete deck
url: "{{baseUrl}}/api/decks/{{deckId}}"
method: DELETE
scripts:
  - type: afterResponse
    code: |-
      pm.test("Status is 204 OK", function () {
          pm.response.to.have.status(204);
      });
    language: text/javascript
order: 4169907858974676
