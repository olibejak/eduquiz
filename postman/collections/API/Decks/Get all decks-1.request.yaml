$kind: http-request
name: Get all decks
url: "{{baseUrl}}/api/decks?page=0&size=10"
method: GET
queryParams:
  page: "0"
  size: "10"
scripts:
  - type: afterResponse
    code: |-
      pm.test("Status is 200 OK", function () {
          pm.response.to.have.status(200);
      });
    language: text/javascript
order: 1360044550242088
