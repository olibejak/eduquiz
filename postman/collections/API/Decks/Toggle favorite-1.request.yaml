$kind: http-request
name: Toggle favorite
url: "{{baseUrl}}/api/decks/{{deckId}}/favorite"
method: POST
scripts:
  - type: afterResponse
    code: |-
      pm.test("Status is 200 OK", function () {
          pm.response.to.have.status(200);
      });
    language: text/javascript
order: 4044960148175777
