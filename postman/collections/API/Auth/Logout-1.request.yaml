$kind: http-request
name: Logout
url: "{{baseUrl}}/api/auth/logout"
method: POST
headers:
  ngrok-skip-browser-warning: "1"
auth:
  type: noauth
scripts:
  - type: afterResponse
    code: |-
      pm.test("Status is 200 OK", function () {
          pm.response.to.have.status(200);
      });
    language: text/javascript
order: 3296077993864396
