$kind: collection
name: API
scripts:
  - type: http:afterResponse
    code: |-
      pm.test("Response is faster than 500 ms", function () {
          pm.expect(pm.response.responseTime).to.be.below(500);
      });
